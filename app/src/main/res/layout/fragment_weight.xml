<?xml version="1.0" encoding="utf-8"?>

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".starter.WeightFragment" >


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorBackground"
        android:orientation="vertical">

        <TextView
            android:id="@+id/weight_text"
            style="@style/TextViewQuestionStyle"
            android:text="@string/weight_ques" />


        <LinearLayout
            android:id="@+id/weight_layout"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:layout_below="@id/weight_text"
            android:gravity="center"
            android:orientation="horizontal">


            <EditText
                android:id="@+id/enter_age"
                style="@style/EditTextFragment"
                android:hint="@string/age_num"
                android:inputType="number" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:layout_marginEnd="25dp"
                android:gravity="center"
                android:text="kg"
                android:textColor="@color/textColor"
                android:textSize="30sp" />


        </LinearLayout>

        <Button
            android:id="@+id/done_btn_name"
            style="@style/FragmentButtonStyle"
            android:text="@string/done_btn" />

    </RelativeLayout>

</layout>